import React from "react";
import ReactDOM from "react-dom/client";
import LUMENADashboard from "./LUMENADashboard";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LUMENADashboard />
  </React.StrictMode>
);
